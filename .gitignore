node_modules

dist
dist-test
*.tsbuildinfo

*.log
npm-debug.log
coverage
.nyc_output

package-lock.json
yarn.lock

*~
.idea/
.DS_Store

# Note: ts/test/csmith-fixtures/*.json.gz ARE tracked — they are the
# CSmith regression fixtures the csmith test replays.

# Vale's downloaded style packages. The vocabulary under
# .vale/styles/config/ and the house rule under .vale/styles/Tabnas/
# are ours and ARE committed; Google/ arrives from the pinned URL
# in .vale.ini via `vale sync`.
/.vale/styles/Google/

# Written by the counts script while measuring, removed after.
.vale-counts.ini

# Cargo build output. The global cargo config on the dev machines points
# target-dir elsewhere, but a default `cargo build` writes here.
rs/target/
