{
  "name": "@tabnas/c",
  "version": "0.2.1",
  "description": "This plugin allows the [Tabnas](https://github.com/tabnas/parser) parser to parse C source into a concrete syntax tree, preserving macros and compiler extensions.",
  "main": "dist/c.js",
  "type": "commonjs",
  "types": "dist/c.d.ts",
  "homepage": "https://github.com/tabnas/c",
  "keywords": [
    "c",
    "parser",
    "concrete-syntax-tree",
    "cst",
    "tabnas",
    "jsonic"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/c.git"
  },
  "scripts": {
    "test": "node --enable-source-maps --test \"dist-test/*.test.js\"",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test \"dist-test/*.test.js\"",
    "embed": "node embed-grammar.js",
    "watch": "tsc --build src test -w",
    "build": "node embed-grammar.js && tsc --build src test",
    "clean": "rm -rf dist dist-test node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag && npm publish --access public --registry https://registry.npmjs.org "
  },
  "license": "MIT",
  "files": [
    "src",
    "dist",
    "c-grammar.jsonic",
    "embed-grammar.js",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@tabnas/expr": "file:../../expr/ts",
    "@tabnas/jsonic": "file:../../jsonic/ts",
    "@tabnas/parser": "file:../../parser/ts",
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  },
  "peerDependencies": {
    "@tabnas/expr": "^0.2.0",
    "@tabnas/jsonic": "^0.2.0",
    "@tabnas/parser": "^0.2.0"
  },
  "engines": {
    "node": ">=24"
  }
}
